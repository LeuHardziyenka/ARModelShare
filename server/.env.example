# Supabase Configuration
# Get these values from your Supabase project settings
# Supabase Console: https://app.supabase.com/
# Project Settings > API

# Supabase Project URL (e.g., https://xxxxx.supabase.co)
VITE_SUPABASE_URL=your_supabase_url_here

# Supabase Anonymous Key (public anon key - safe to expose in client)
VITE_SUPABASE_ANON_KEY=your_supabase_anon_key_here

# Supabase Service Role Key (KEEP SECRET - for backend only)
# This key bypasses Row Level Security - NEVER expose to client
# Find in: Supabase Console > Project Settings > API > service_role key
SUPABASE_SERVICE_KEY=your_supabase_service_role_key_here

# API Configuration
# Leave empty to use same origin as frontend
VITE_API_URL=

# Server Configuration (Optional)
PORT=5000

# Note: For Vite to pick up VITE_* variables, they must be in client/.env.local
# Server-side variables (SUPABASE_SERVICE_KEY) should be in root .env.local
