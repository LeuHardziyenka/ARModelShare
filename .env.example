# Supabase Configuration
# Get these values from your Supabase project settings
# Supabase Console: https://app.supabase.com/
# Project Settings > API

# Frontend (Vite) variables - prefixed with VITE_
VITE_SUPABASE_URL=your_supabase_url_here
VITE_SUPABASE_ANON_KEY=your_supabase_anon_key_here

# Backend (Node.js) variables - same values, no VITE_ prefix
SUPABASE_URL=your_supabase_url_here
SUPABASE_ANON_KEY=your_supabase_anon_key_here

# Supabase Service Role Key (KEEP SECRET - for backend only!)
# This key bypasses Row Level Security - NEVER expose to frontend
# Find in: Supabase Console > Project Settings > API > service_role key
SUPABASE_SERVICE_KEY=your_service_role_key_here

# API Configuration (optional)
# Leave empty to use same origin as frontend
VITE_API_URL=

# Server Configuration
PORT=5000

# Note:
# - dotenv loads .env by default
# - Vite also loads .env and exposes VITE_* variables to frontend
# - Both VITE_* (frontend) and non-VITE (backend) versions are needed
